language: go
sudo: false
go:
- 1.7.x
- 1.8.x
- tip
install:
 - go get github.com/kyoh86/richgo
 - go get -t
script: richgo test -v -coverprofile=coverage.txt -covermode=atomic
after_success: bash <(curl -s https://codecov.io/bash)
